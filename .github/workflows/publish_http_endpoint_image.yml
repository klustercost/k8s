name: Create and publish the simple http endpoint image

# Configures this workflow to run every time a change is pushed to main.
on:
  push:
    branches:
      - main
    paths:
      - sql/postgres/certificate/http/**
  workflow_dispatch:      
jobs:
  build-and-push-image:
    # Sets the permissions granted to the `GITHUB_TOKEN` for the actions in this job.
    permissions:
      packages: write
    uses: ./.github/workflows/base_build_image.yaml
    with:
      registry: ghcr.io
      image_name: klustercost-http-endpoint
      base_folder: sql/postgres/certificate/http
    secrets: 
      token: ${{ secrets.GITHUB_TOKEN }}
