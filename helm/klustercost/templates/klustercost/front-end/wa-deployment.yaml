{{- if .Values.whatsappfrontend.enabled }}
kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ .Release.Name }}-whatsapp-frontend
  labels:
    {{- include "klustercost.componentLabels" (dict "context" . "component" "frontend") | nindent 4 }}
spec:
  replicas: {{ .Values.whatsappfrontend.replicas }}
  selector:
    matchLabels:
      {{- include "klustercost.componentSelectorLabels" (dict "context" . "component" "frontend") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "klustercost.componentLabels" (dict "context" . "component" "frontend") | nindent 8 }}
    spec:
      containers:
        - name: kc-wab
          image: {{ .Values.whatsappfrontend.image }}
          env:
            - name: VERIFY_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-whatsapp-frontend
                  key: VERIFY_TOKEN
            - name: ACCESS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-whatsapp-frontend
                  key: ACCESS_TOKEN
            - name: HOST_PORT
              value: "{{ .Values.whatsappfrontend.targetPort }}"
            - name: MCP_CLIENT_ADDRESS
              value: http://{{ .Release.Name }}-mcp-client.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.mcp.client.port }}
            - name: TEMPLATE
              value: {{ .Values.whatsappfrontend.template | quote }}
            - name: TEMPLATE_CONTEXT
              value: {{ .Values.whatsappfrontend.templateContext | quote }}
          resources:
            requests:
              cpu: {{ .Values.whatsappfrontend.resources.requests.cpu }}
              memory: {{ .Values.whatsappfrontend.resources.requests.memory }}
            limits:
              cpu: {{ .Values.whatsappfrontend.resources.limits.cpu }}
              memory: {{ .Values.whatsappfrontend.resources.limits.memory }}
          ports:
            - containerPort: {{ .Values.whatsappfrontend.targetPort }}
              protocol: TCP   
          readinessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.whatsappfrontend.targetPort }}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.whatsappfrontend.targetPort }}
            initialDelaySeconds: 10
            periodSeconds: 30                     
{{- end }}
