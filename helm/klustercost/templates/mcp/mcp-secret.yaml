{{- if .Values.mcp.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-mcp-secret
  labels:
    app: {{ .Release.Name }}-mcp
type: Opaque
stringData:
  OPENAI_API_KEY: {{ .Values.mcp.openai.apiKey | quote }}
{{- end }}
